spring:
  cloud:
    kubernetes:
      reload:
        period: 1000
        enabled: true
#        mode: event
      config:
        enabled: true
        name: comment-service
        namespace: tweetlocal
        sources:
          - name: comment-service
      enabled: true
#        monitoring-config-maps: true
#        mode: event