spring:
  cloud:
    kubernetes:
      reload:
        enabled: true
        monitoring-config-maps: true
      #        mode: event
      config:
        enabled: true
#        name: comment-service
#        namespace: tweetlocal
#        sources:
#          - name: comment-service
      enabled: true
#        monitoring-config-maps: true
#        mode: event
server:
  port: 8082