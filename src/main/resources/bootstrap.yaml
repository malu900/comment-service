spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        name: post-service
        namespace: tweetlocal
        sources:
#          - namespace: tweetlocal
          - name: post-service
      reload:
        enabled: true
        mode: event
      enabled: true
#        monitoring-config-maps: true
#        mode: event